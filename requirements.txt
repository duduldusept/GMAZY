Django==5.2.16
asgiref==3.12.1
sqlparse==0.5.5
tzdata==2026.3
gunicorn
dj-database-url
psycopg2-binary

# Optionnel mais recommandé : permet de charger automatiquement le fichier
# .env (voir .env.example). Sans lui, les variables d'environnement
# système sont utilisées directement.
python-dotenv

# Nécessaires pour le déploiement en production (voir GUIDE_DEPLOIEMENT.md),
# mais aussi utilisées par settings.py en développement (whitenoise est dans
# MIDDLEWARE dans tous les cas) : à garder installées même pour lancer_serveur.bat.
waitress==3.0.2
whitenoise==6.12.0
